id: azure.keyvault.get_secret
name: Azure Key Vault Get Secret
description: Retrieve a secret from Azure Key Vault.
version: "0.1.0"
timeout_sec: 30
steps:
  get:
    id: get
    name: Key Vault Get Secret
    type: worker
    topic: job.azure.read
    input:
      profile: ${input.profile}
      action: keyvault.get_secret
      params: ${input.params}
      request_id: ${input.request_id}
    input_schema_id: azure/AzureKeyVaultGetSecretInput
    output_schema_id: azure/AzureActionResult
    meta:
      pack_id: azure
      capability: azure.read
      risk_tags: ["read", "network"]
      requires: ["network:egress"]
