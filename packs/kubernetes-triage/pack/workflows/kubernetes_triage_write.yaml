id: kubernetes-triage.write
name: Kubernetes Triage Write
description: Perform approved Kubernetes remediation actions.
version: "0.1.0"
timeout_sec: 120
steps:
  write:
    id: write
    name: Kubernetes Triage Write action
    type: worker
    topic: job.kubernetes-triage.write
    input:
      profile: ${input.profile}
      action: ${input.action}
      params: ${input.params}
      request_id: ${input.request_id}
    input_schema_id: kubernetes-triage/KubernetesTriageActionInput
    output_schema_id: kubernetes-triage/KubernetesTriageActionResult
    meta:
      pack_id: kubernetes-triage
      capability: kubernetes-triage.write
      risk_tags: ["write", "network"]
      requires: ["network:egress"]
