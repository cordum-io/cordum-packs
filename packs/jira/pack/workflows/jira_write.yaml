id: jira.write
name: Jira Write
description: Create or transition Jira issues.
version: "0.1.0"
timeout_sec: 120
steps:
  write:
    id: write
    name: Jira Write action
    type: worker
    topic: job.jira.write
    input:
      profile: ${input.profile}
      action: ${input.action}
      params: ${input.params}
      request_id: ${input.request_id}
      auth: ${input.auth}
    input_schema_id: jira/JiraActionInput
    output_schema_id: jira/JiraActionResult
    meta:
      pack_id: jira
      capability: jira.write
      risk_tags: ["write", "network"]
      requires: ["network:egress"]
