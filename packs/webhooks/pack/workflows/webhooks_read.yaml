id: webhooks.read
name: Webhooks Read
description: Receive and validate inbound webhook payloads.
version: "0.1.0"
timeout_sec: 60
steps:
  read:
    id: read
    name: Webhooks Read action
    type: worker
    topic: job.webhooks.read
    input:
      profile: ${input.profile}
      action: ${input.action}
      params: ${input.params}
      request_id: ${input.request_id}
    input_schema_id: webhooks/WebhooksActionInput
    output_schema_id: webhooks/WebhooksActionResult
    meta:
      pack_id: webhooks
      capability: webhooks.read
      risk_tags: ["read"]
      requires: []
